if game.PlaceId == 139769003880269 then

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- ✅ generateID function at the top
local function generateID()
   local template = "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx"
   return string.gsub(template, "[xy]", function(c)
      local v = (c == "x") and math.random(0, 0xf) or math.random(8, 0xb)
      return string.format("%x", v)
   end)
end

local Window = Rayfield:CreateWindow({
   Name = "Kaiju Alpha",
   LoadingTitle = "Kaiju Alpha",
   LoadingSubtitle = "by Neptune",
   DisableRayfieldPrompts = true,
   DisableBuildWarnings = true,
   ConfigurationSaving = {
      Enabled = false,
      FolderName = nil,
      FileName = "Neptune67Godzilla"
   },
   Discord = {
      Enabled = false,
      Invite = "",
      RememberJoins = true
   },
   KeySystem = false,
   KeySettings = {
      Title = "Sirius Hub",
      Subtitle = "Key System",
      Note = "Join the discord (discord.gg/sirius)",
      FileName = "SiriusKey",
      SaveKey = true,
      GrabKeyFromSite = false,
      Key = "Hello"
   }
})

local MainTab = Window:CreateTab("🦎Main", nil)
local MainSection = MainTab:CreateSection("Main", true)

Rayfield:Notify({
   Title = "Kaiju Alpha",
   Content = "Cool execution",
   Duration = 3,
   Image = nil,
   Actions = {
      Ignore = {
         Name = "Okay!",
         Callback = function()
            print("Executed!")
         end
      },
   },
})

local isFlying = false
MainTab:CreateToggle({
   Name = "Fly",
   Info = "Toggles flying on your model",
   CurrentValue = false,
   Flag = "FlyToggle",
   Callback = function(Value)
      isFlying = Value
      local player = game:GetService("Players").LocalPlayer
      local liveFolder = workspace:FindFirstChild("Live")
      if liveFolder then
         local model = liveFolder:FindFirstChild(player.Name)
         if model then
            model:SetAttribute("IsFlying", Value)
            if Value then
               model:SetAttribute("ServerSpeed", 25)
               model:SetAttribute("SpeedMulti", 25)
               model:SetAttribute("UseServerSpeed", true)
            end
         end
      end
   end,
})

local isRunning = false
MainTab:CreateToggle({
   Name = "Inf Stam",
   Info = "Spams you with 1000000 stamina",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      isRunning = Value
      if Value then
         task.spawn(function()
            while isRunning do
               local args = {"GetEnergy", -1000000}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("GetModel"):InvokeServer(unpack(args))
               task.wait()
            end
         end)
      end
   end,
})

local ishealing = false
MainTab:CreateToggle({
   Name = "Inf Health",
   Info = "Spams you with 100000 Health",
   CurrentValue = false,
   Flag = "Toggle67",
   Callback = function(Value)
      ishealing = Value
      if Value then
         task.spawn(function()
            while ishealing do
               local args = {generateID() .. "_19", "UpdateHealth", 10000000}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("FireServer"):FireServer(unpack(args))
               task.wait()
            end
         end)
      end
   end,
})

MainTab:CreateSlider({
   Name = "WalkSpeed Slider",
   Info = "Changes speed attribute directly on your model",
   Range = {0, 500},
   Increment = 1,
   Suffix = "Speed",
   CurrentValue = 0,
   Flag = "Slider1",
   Callback = function(Value)
      local args = {generateID() .. "_19", "UpdateSpeed", true, Value}
      game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CombatRemote"):FireServer(unpack(args))
      local player = game:GetService("Players").LocalPlayer
      local liveFolder = workspace:FindFirstChild("Live")
      if liveFolder then
         local model = liveFolder:FindFirstChild(player.Name)
         if model then
            model:SetAttribute("ServerSpeed", Value)
            model:SetAttribute("SpeedMulti", Value)
         end
      end
   end,
})

MainTab:CreateInput({
   Name = "Damage Multi(set to 1 for normal damage)",
   Info = "Damage Multi",
   PlaceholderText = "Enter damage multiplier...",
   NumbersOnly = true,
   OnEnter = true,
   RemoveTextAfterFocusLost = false,
   Flag = "Input1",
   Callback = function(Text)
      local Value = tonumber(Text)
      if Value then
         local args = {generateID() .. "_19", "UpdateAttribute", "DamageMulti", Value}
         game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("FireServer"):FireServer(unpack(args))
      end
   end,
})

local Button = MainTab:CreateButton({
   Name = "Quick Menu",
   Info = "Goes to menu without the wait",
   Interact = "Changable",
   Callback = function()
      local args = {generateID() .. "_19", "LoadCharacter"}
      game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("FireServer"):FireServer(unpack(args))
   end,
})

task.wait(0.1)

local Main1Tab = Window:CreateTab("attacks", nil)
local Main1Section = Main1Tab:CreateSection("attacks", true)

local isAttacking1 = false
Main1Tab:CreateToggle({
   Name = "Spam Skill 1",
   Info = "Automatically spams Attack1",
   CurrentValue = false,
   Flag = "Toggle2",
   Callback = function(Value)
      isAttacking1 = Value
      if Value then
         task.spawn(function()
            while isAttacking1 do
               local args = {generateID() .. "_19", "CombatFunction", "NormalHit", "Attack1"}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CombatRemote"):FireServer(unpack(args))
               task.wait(0.001)
            end
         end)
      end
   end,
})

local isAttacking2 = false
Main1Tab:CreateToggle({
   Name = "Spam Skill 2",
   Info = "Automatically spams Attack2",
   CurrentValue = false,
   Flag = "Toggle3",
   Callback = function(Value)
      isAttacking2 = Value
      if Value then
         task.spawn(function()
            while isAttacking2 do
               local args = {generateID() .. "_19", "CombatFunction", "NormalHit", "Attack2"}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CombatRemote"):FireServer(unpack(args))
               task.wait(0.001)
            end
         end)
      end
   end,
})

local isAttacking3 = false
Main1Tab:CreateToggle({
   Name = "Spam Skill 3",
   Info = "Automatically spams Attack3",
   CurrentValue = false,
   Flag = "Toggle4",
   Callback = function(Value)
      isAttacking3 = Value
      if Value then
         task.spawn(function()
            while isAttacking3 do
               local args = {generateID() .. "_19", "CombatFunction", "NormalHit", "Attack3"}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CombatRemote"):FireServer(unpack(args))
               task.wait(0.001)
            end
         end)
      end
   end,
})

local isAttacking4 = false
Main1Tab:CreateToggle({
   Name = "Spam Skill 4",
   Info = "Automatically spams Attack4",
   CurrentValue = false,
   Flag = "Toggle5",
   Callback = function(Value)
      isAttacking4 = Value
      if Value then
         task.spawn(function()
            while isAttacking4 do
               local args = {generateID() .. "_19", "CombatFunction", "NormalHit", "Attack4"}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CombatRemote"):FireServer(unpack(args))
               task.wait(0.000000001)
            end
         end)
      end
   end,
})

local isAttacking5 = false
Main1Tab:CreateToggle({
   Name = "Spam Skill 5",
   Info = "Automatically spams Attack5",
   CurrentValue = false,
   Flag = "Toggle6",
   Callback = function(Value)
      isAttacking5 = Value
      if Value then
         task.spawn(function()
            while isAttacking5 do
               local args = {generateID() .. "_19", "CombatFunction", "NormalHit", "Attack5"}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CombatRemote"):FireServer(unpack(args))
               task.wait(0.001)
            end
         end)
      end
   end,
})

task.wait(0.1)

local KaijusTab = Window:CreateTab("Kaijus", nil)
local KaijusSection = KaijusTab:CreateSection("Kaijus", true)

local kaijuList = {
   "Anguirus", "Gojira 1954", "Mothra Larva", "Showa Rodan",
   "Heisei Godzilla", "Junior Godzilla", "Biollante", "Volcanic Biollante",
   "Burning Godzilla", "Kamata Kun", "Shinagawa Kun", "Shin Godzilla",
   "Godzilla 2000", "Orga (First Form)", "Orga (Second Form)", "Kiryu Type 3",
   "Kong 2017", "Godzilla 2014", "Godzilla 2019", "Titan X",
   "Mecha Godzilla 2021", "Godzilla 2021", "Male Muto", "Female Muto",
   "Thermo Godzilla", "Stirker Eureka", "Frostbite Godzilla",
   "Minilla", "Super Goji", "Godzilla Minus One"
}

for _, kaijuName in pairs(kaijuList) do
   KaijusTab:CreateButton({
      Name = kaijuName,
      Info = "Spawn " .. kaijuName,
      Interact = "Spawn",
      Callback = function()
         local args = {generateID() .. "_19", "Spawn", kaijuName, "Tokyo"}
         game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("FireServer"):FireServer(unpack(args))
      end,
   })
end

-- Evolved Godzilla with camera fix
KaijusTab:CreateButton({
   Name = "Evolved Godzilla",
   Info = "Spawn Evolved Godzilla",
   Interact = "Spawn",
   Callback = function()
      local args = {generateID() .. "_19", "Spawn", "Evolved Godzilla", "Tokyo"}
      game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("FireServer"):FireServer(unpack(args))
      task.wait(2)
      local player = game:GetService("Players").LocalPlayer
      local character = player.Character or player.CharacterAdded:Wait()
      local humanoid = character:WaitForChild("Humanoid", 5)
      if humanoid then
         workspace.CurrentCamera.CameraSubject = humanoid
         workspace.CurrentCamera.CameraType = Enum.CameraType.Follow
      end
   end,
})

task.wait(0.1)

local VFXTab = Window:CreateTab("VFX(MUST HAVE THE KAIJU ON)", nil)
local VFXSection = VFXTab:CreateSection("VFX(MUST HAVE THE KAIJU ON)", true)

local Button = VFXTab:CreateButton({
   Name = "Nuke All Buildings(ANY KAIJU)",
   Info = "Fires atomic at every building at once",
   Interact = "Fire",
   Callback = function()
      local buildings = workspace:FindFirstChild("Building"):GetChildren()
      for _, building in pairs(buildings) do
         if building:IsA("Model") and building.PrimaryPart then
            task.spawn(function()
               local pos = building.PrimaryPart.Position
               local args = {
                  "Godzilla Minus One", "Atomic",
                  game:GetService("Players").LocalPlayer.Character,
                  vector.create(pos.X, pos.Y, pos.Z)
               }
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("VisualRemote"):FireServer(unpack(args))
            end)
         end
      end
   end,
})

local Button = VFXTab:CreateButton({
   Name = "Female Muto Nuke",
   Info = "Fires 5 Muto EMP per building",
   Interact = "Fire",
   Callback = function()
      local buildings = workspace:FindFirstChild("Building"):GetChildren()
      for _, building in pairs(buildings) do
         if building:IsA("Model") and building.PrimaryPart then
            task.spawn(function()
               local pos = building.PrimaryPart.Position
               for i = 1, 5 do
                  local args = {"Female Muto", "EMP", game:GetService("Players").LocalPlayer.Character, vector.create(pos.X, pos.Y, pos.Z)}
                  game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("VisualRemote"):FireServer(unpack(args))
               end
            end)
         end
      end
   end,
})

local Button = VFXTab:CreateButton({
   Name = "Stirker Nuke(May freeze for a while)",
   Info = "Fires 5 Stirker Missiles per building",
   Interact = "Fire",
   Callback = function()
      local buildings = workspace:FindFirstChild("Building"):GetChildren()
      for _, building in pairs(buildings) do
         if building:IsA("Model") and building.PrimaryPart then
            task.spawn(function()
               local pos = building.PrimaryPart.Position
               for i = 1, 5 do
                  local args = {"Stirker Eureka", "Missile", game:GetService("Players").LocalPlayer.Character, vector.create(pos.X, pos.Y, pos.Z)}
                  game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("VisualRemote"):FireServer(unpack(args))
               end
            end)
         end
      end
   end,
})

local Button = VFXTab:CreateButton({
   Name = "Frostbite Nuke",
   Info = "Fires 5 Frostbite Beams per building",
   Interact = "Fire",
   Callback = function()
      local buildings = workspace:FindFirstChild("Building"):GetChildren()
      for _, building in pairs(buildings) do
         if building:IsA("Model") and building.PrimaryPart then
            task.spawn(function()
               local pos = building.PrimaryPart.Position
               for i = 1, 5 do
                  local args = {"Frostbite Godzilla", "Beam", game:GetService("Players").LocalPlayer.Character, vector.create(pos.X, pos.Y, pos.Z)}
                  game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("VisualRemote"):FireServer(unpack(args))
               end
            end)
         end
      end
   end,
})

local Button = VFXTab:CreateButton({
   Name = "Titan X Nuke",
   Info = "Fires 5 Titan Bubbles per building",
   Interact = "Fire",
   Callback = function()
      local buildings = workspace:FindFirstChild("Building"):GetChildren()
      for _, building in pairs(buildings) do
         if building:IsA("Model") and building.PrimaryPart then
            task.spawn(function()
               local pos = building.PrimaryPart.Position
               for i = 1, 5 do
                  local args = {"Titan X", "Bubble", game:GetService("Players").LocalPlayer.Character, vector.create(pos.X, pos.Y, pos.Z)}
                  game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("VisualRemote"):FireServer(unpack(args))
               end
            end)
         end
      end
   end,
})

local isMutoSpamming = false
VFXTab:CreateToggle({
   Name = "Female Muto Spam",
   Info = "Spams muto VFX",
   CurrentValue = false,
   Flag = "Toggle2",
   Callback = function(Value)
      isMutoSpamming = Value
      if Value then
         task.spawn(function()
            while isMutoSpamming do
               local args = {"Female Muto", "EMP", game:GetService("Players").LocalPlayer.Character}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("VisualRemote"):FireServer(unpack(args))
               task.wait()
            end
         end)
      end
   end,
})

local isStirkerSpamming = false
VFXTab:CreateToggle({
   Name = "Stirker Spam Missle",
   Info = "Spams Stirker VFX",
   CurrentValue = false,
   Flag = "Toggle3",
   Callback = function(Value)
      isStirkerSpamming = Value
      if Value then
         task.spawn(function()
            while isStirkerSpamming do
               local args = {"Stirker Eureka", "Missile", game:GetService("Players").LocalPlayer.Character}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("VisualRemote"):FireServer(unpack(args))
               task.wait()
            end
         end)
      end
   end,
})

local isFGSpamming = false
VFXTab:CreateToggle({
   Name = "Frostbite Godzilla Spam Ice",
   Info = "Spams Frostbite Godzilla VFX",
   CurrentValue = false,
   Flag = "Toggle4",
   Callback = function(Value)
      isFGSpamming = Value
      if Value then
         task.spawn(function()
            while isFGSpamming do
               local args = {"Frostbite Godzilla", "Beam", game:GetService("Players").LocalPlayer.Character}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("VisualRemote"):FireServer(unpack(args))
               task.wait()
            end
         end)
      end
   end,
})

local isBubbleSpamming = false
VFXTab:CreateToggle({
   Name = "Titan X Domain Expansion",
   Info = "Spams Titan VFX",
   CurrentValue = false,
   Flag = "Toggle5",
   Callback = function(Value)
      isBubbleSpamming = Value
      if Value then
         task.spawn(function()
            while isBubbleSpamming do
               local args = {"Titan X", "Bubble", game:GetService("Players").LocalPlayer.Character}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("VisualRemote"):FireServer(unpack(args))
               task.wait()
            end
         end)
      end
   end,
})

task.wait(0.1)

local MiscTab = Window:CreateTab("Misc", nil)
local MiscSection = MiscTab:CreateSection("Misc", true)

local isLooping = false
MiscTab:CreateToggle({
   Name = "Lag Ball Spam",
   Info = "Spams Balls",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      isLooping = Value
      if Value then
         task.spawn(function()
            while isLooping do
               local args = {"Frostbite Godzilla", "Storm", game:GetService("Players").LocalPlayer.Character}
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("VisualRemote"):FireServer(unpack(args))
               task.wait(0.1)
            end
         end)
      end
   end,
})

task.wait(0.1)

local BeamsTab = Window:CreateTab("Beam", nil)
local BeamsSection = BeamsTab:CreateSection("Beam(Requires Beam)", true)

local isLooping2 = false
BeamsTab:CreateToggle({
   Name = "Break People Beam",
   Info = "Break People Beam",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      isLooping2 = Value
      if Value then
         task.spawn(function()
            while isLooping2 do
               local args = {
                  generateID() .. "_19",
                  "CombatFunction", "CreateBeam",
                  {
                     Knockback = {FaceTarget = false, Velocity = -1, YVelocity = 0, Duration = 1},
                     Name = "Atomic", Energy = 0,
                     HitboxData = {
                        Offset = CFrame.new(0, 0, 10000, 1, 0, 0, 0, 1, 0, 0, 0, 1),
                        IsContinuous = false, BasePart = "HumanoidRootPart",
                        Duration = 0.1, Size = vector.create(210000, 2500000000, 2500000000011)
                     },
                     Cooldown = 0, CanMove = true, Damage = NumberRange.new(0, 0)
                  }
               }
               game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CombatRemote"):FireServer(unpack(args))
               task.wait(0.0000000000000000000000000001)
            end
         end)
      end
   end,
})

BeamsTab:CreateButton({
   Name = "Fling Beam",
   Info = "Flings",
   Interact = "Changable",
   Callback = function()
      local args = {
         generateID() .. "_19",
         "CombatFunction", "CreateBeam",
         {
            Knockback = {FaceTarget = false, Velocity = 23432432432432432432423423423, YVelocity = 4234243243242424242423432423423423423, Duration = 1},
            Name = "Atomic", Energy = 0,
            HitboxData = {
               Offset = CFrame.new(0, 0, 10000, 1, 0, 0, 0, 1, 0, 0, 0, 1),
               IsContinuous = false, BasePart = "HumanoidRootPart",
               Duration = 1, Size = vector.create(210000, 2500000000, 2500000000011)
            },
            Cooldown = 0, CanMove = true, Damage = NumberRange.new(0, 0)
         }
      }
      game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CombatRemote"):FireServer(unpack(args))
   end,
})

BeamsTab:CreateButton({
   Name = "Fling Beam",
   Info = "Flings",
   Interact = "Changable",
   Callback = function()
      local args = {
         generateID() .. "_43",
         "CombatFunction", "CreateBeam",
         {
            Knockback = {
               FaceTarget = false,
               Velocity = 23432432432432432432423423423,
               YVelocity = 4234243243242424242423432423423423423,
               Duration = 1
            },
            Name = "Atomic",
            Energy = 50,
            HitboxData = {
               Offset = CFrame.new(0, 0, 0, 1, 0, 0, 0, 1, 0, 0, 0, 1),
               IsContinuous = false,
               BasePart = "HumanoidRootPart",
               Duration = 0.1,
               Size = vector.create(210000, 2500000000, 2500000000011)
            },
            Cooldown = 0,
            CanMove = true,
            Damage = NumberRange.new(0, 0)
         }
      }
      game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CombatRemote"):FireServer(unpack(args))
   end,
})

task.wait(0.1)

local TeleTab = Window:CreateTab("Teleports", nil)
local TeleSection = TeleTab:CreateSection("Teleports", true)

TeleTab:CreateButton({
   Name = "Hong Kong",
   Info = "Teleport to Hong Kong",
   Interact = "Teleport",
   Callback = function()
      game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(866.581177, 42.75, -820.61731, -0.99619168, 8.70898447e-08, 0.0871905237, 9.50452161e-08, 1, 8.70898447e-08, -0.0871905237, 9.50452161e-08, -0.99619168)
   end,
})

TeleTab:CreateButton({
   Name = "Tokyo",
   Info = "Teleport to Tokyo",
   Interact = "Teleport",
   Callback = function()
      game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(46.000782, 38.0375137, -264.450623, 0, 0, -1, 0, 1, 0, 1, 0, 0)
   end,
})

TeleTab:CreateButton({
   Name = "Forest",
   Info = "Teleport to Forest",
   Interact = "Teleport",
   Callback = function()
      game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-626.250061, 73.8499985, -587.599976, 1, 0, 0, 0, 1, 0, 0, 0, 1)
   end,
})

end
